// const GameState = Object.freeze({
//   PRE_INCEPTION: Symbol("Game has not started"),
//   GAME_ON: Symbol("The game is ON !!"),
//   GAME_OVER: Symbol("Game over")
// });

const GameState = {
  PRE_INCEPTION: "Game has not started",
  GAME_ON: "The game is ON !!",
  GAME_OVER: "Game over"
};

export default GameState;
